<?xml version='1.0' encoding='utf-8' standalone='yes'?>


<!-- DO NOT EDIT THIS FILE ... YOU WILL LOSE YOUR WORK -->


<c74object name='mutap.aec~' category='audio,  adaptive,  echo,  cleaning'>

	<digest>Acoustic echo canceller</digest>
	<description>Acoustic echo canceller. Subtracts an adaptive estimate of the loudspeaker-to-microphone echo path from the microphone signal — the open-loop cousin of mutap.afc~, for the case where a clean far-end reference exists. Adaptation runs on a PEM-prewhitened signal pair, so it survives double-talk without a double-talk detector (FDAF-PEM, MuTap pem_afc). Inlet 1 takes the microphone, inlet 2 the far-end signal feeding the loudspeaker; the cleaned output is delayed by @block samples. The right outlet reports the IPC double-talk indicator (0..1). @warp selects the frequency-warped near-end model for music/tonal sources; @kalman selects the frequency-domain Kalman engine (v2), the measured double-talk winner. @postfilter selects the residual-echo post-filter engine: 1 engages the full measured AEC chain — raw Kalman canceller, coherence-driven residual suppressor, comfort noise, initial receive guard — the configuration MuTap's ITU-T compliance battery certifies; 2 swaps the suppressor for a small trained network (see @model). </description>


	<!--METADATA-->

	<metadatalist>
		<metadata name='author'>MuTap contributors </metadata>
		<metadata name='tag'>audio</metadata>
		<metadata name='tag'>adaptive</metadata>
		<metadata name='tag'>echo</metadata>
		<metadata name='tag'>cleaning</metadata>
	</metadatalist>


	<!--ARGUMENTS-->

	<objarglist>
		<objarg name='filter-length' optional='1' type='int'>
			<digest>Echo-path filter length in samples</digest>
			<description>Echo-path filter length in samples (default 2048). Together with the block size it sets the partition count: partitions = ceil(filter_length / block). </description>
		</objarg>
	</objarglist>


	<!--MESSAGES-->

	<methodlist>
		<method name='reset'>
			<digest>Reset the canceller</digest>
			<description>Reset the canceller: zero the learned echo-path estimate. </description>
		</method>
	</methodlist>


	<!--ATTRIBUTES-->

	<attributelist>

		<attribute name='block' get='1' set='1' type='long' size='1' >
			<digest>Canceller block size in samples</digest>
			<description>Canceller block size in samples (rounded up to a power of 2, 16-4096). Sets the adaptation hop, the added output latency, and — with the filter-length creation arg — the partition count. Changing it rebuilds the canceller from scratch (the learned filter resets). </description>
		</attribute>

		<attribute name='mu' get='1' set='1' type='float64' size='1' >
			<digest>NLMS adaptation step size</digest>
			<description>NLMS adaptation step size, clamped to (0, 2). 0.5 is a robust default; smaller adapts slower but tracks more calmly. Applied live (no rebuild). </description>
		</attribute>

		<attribute name='adapt' get='1' set='1' type='long' size='1' >
			<digest>Enable adaptation</digest>
			<description>Enable adaptation. Off freezes the learned echo-path estimate; cancellation keeps running with the frozen filter. </description>
		</attribute>

		<attribute name='gate' get='1' set='1' type='long' size='1' >
			<digest>Double-talk robustness layer (classic engine)</digest>
			<description>Robustness layer for double-talk on the classic engine: scale the step size by IPC^2 and skip updates on near-end transients (transient freeze ratio 4). Freezing through double-talk is the classical AEC answer; the PEM prewhitening already removes most of the need, and the Kalman engine all of it. Changing it rebuilds the canceller from scratch (the learned filter resets). With warp on, the IPC step scaling stays on even when gate is off — the warped model requires it. With postfilter on, gate selects the chain's initial receive guard instead (switched send loss until convergence certifies, then latched off permanently). </description>
		</attribute>

		<attribute name='warp' get='1' set='1' type='long' size='1' >
			<digest>Frequency-warped (music/tonal) near-end model</digest>
			<description>Near-end model: off = the speech cascade (short-term LP + pitch tap), on = the frequency-warped LP built for music/tonal sources — sustained low chords whose packed bass partials defeat the speech model. On music-material double-talk it measured the better echo suppression in every room tried (MuTap test_aec.cpp). With the classic engine, warp keeps IPC step scaling on regardless of gate (the warped whitener requires it); with the Kalman engine no pairing is needed. Changing it rebuilds the canceller from scratch (the learned filter resets). </description>
		</attribute>

		<attribute name='kalman' get='1' set='1' type='long' size='1' >
			<digest>Frequency-domain Kalman engine (v2)</digest>
			<description>Adaptive engine: off = the classic NLMS update (mu + gate), on = the frequency-domain Kalman filter (v2) — per-frequency state uncertainty and near-end tracking replace the step size, so mu is ignored and gate selects the burst floor instead. For echo cancellation this is the measured double-talk winner: the near-end PSD it tracks per bin is exactly a double-talk model, no detector needed. The IPC outlet reports 0 with the Kalman engine (its gating is internal). Changing it rebuilds the canceller from scratch (the learned filter resets). </description>
		</attribute>

		<attribute name='postfilter' get='1' set='1' type='long' size='1' >
			<digest>Residual-echo post-filter engine (0 off, 1 classical, 2 learned)</digest>
			<description>Residual-echo post-filter engine: 0 = the bare adaptive canceller selected by the attributes above, 1 = the measured AEC CHAIN — the raw frequency-domain Kalman canceller plus the coherence-driven residual suppressor, comfort noise matched to the near-end noise floor, and the initial receive guard. This is the configuration MuTap's ITU-T compliance battery certifies at 48 and 16 kHz (single-talk residual below -76 dBm0(A), double-talk near-end cost about 1 dB, full-duplex P.340 Category 1), with its time constants rescaled for the actual block size and sample rate. 2 = the LEARNED chain: the same canceller and guard with the post-filter replaced by a small trained network predicting per-band gains (see model) — measured stronger single-talk echo removal on speech-like material at equal near-end transparency, weaker double-talk suppression on material unlike its training data; the classical engine remains the certified default. The learned engine requires block to equal its model's trained block size (256 for the built-in model) and coerces it, with a console notice, if it does not. With postfilter nonzero, mu, warp and kalman are ignored (the chain's canceller is already the Kalman core; PEM buys nothing open-loop) and gate selects the initial receive guard; the right outlet reports the post-filter's echo-explained fraction (0..1) instead of IPC. Adds one extra block of latency (the suppressor's constrained gain filter). Changing it rebuilds the canceller from scratch (the learned filter resets). </description>
		</attribute>
		<attribute name='model' get='1' set='1' type='symbol' size='1' >
			<digest>Learned-engine weights file (postfilter 2 only)</digest>
			<description>Learned-engine weights (postfilter 2 only): a path to a trained MUNN model file (MuTap's tools/ml pipeline exports these), or empty for the package's built-in 48 kHz model. A model carries its own analysis geometry; block must equal its trained block size and is coerced, with a console notice, if it does not. A file that cannot be read or validated posts an error and leaves the running engine unchanged. Changing it rebuilds the canceller from scratch (the learned filter resets). </description>
		</attribute>

		<attribute name='comfort' get='1' set='1' type='long' size='1' >
			<digest>Comfort noise (postfilter only)</digest>
			<description>Comfort noise (postfilter only): fill suppressed bins with noise matched to the tracked near-end noise floor (two-window minimum statistics), so the far end does not hear the room breathe as suppression comes and goes. Fill only — never subtraction. Off leaves suppressed bins silent (G.168's Figure 9 measurement convention). Changing it rebuilds the canceller from scratch (the learned filter resets). </description>
		</attribute>

	</attributelist>


	<!--RELATED-->

	<seealsolist>
		<seealso name='mutap.afc~' />
		<seealso name='adc~' />
		<seealso name='dac~' />
		<seealso name='adoutput~' />
	</seealsolist>


</c74object>
